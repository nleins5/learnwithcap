"use client";
import { SectionEditor } from "../components";

export default function CTAAdmin() {
  return (
    <SectionEditor
      title="CTA Section"
      description="Quản lý phần Call-to-Action — tiêu đề, nút bấm, video background"
      sectionKey="cta_section"
    />
  );
}
