"use client";
import { SectionEditor } from "../components";

export default function HeroAdmin() {
  return (
    <SectionEditor
      title="Hero Section"
      description="Quản lý banner, tiêu đề và video/slider trên trang chủ"
      sectionKey="hero"
    />
  );
}
