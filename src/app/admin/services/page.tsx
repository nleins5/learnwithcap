"use client";
import { SectionEditor } from "../components";

export default function ServicesAdmin() {
  return (
    <SectionEditor
      title="Services (Bạn là)"
      description="Quản lý carousel 'Bạn là' — danh sách đối tượng khách hàng"
      sectionKey="services"
    />
  );
}
