"use client";
import { SectionEditor } from "../components";

export default function SolutionsAdmin() {
  return (
    <SectionEditor
      title="Solutions"
      description="Quản lý phần 'Giải pháp' trên trang chủ — icon, text và header"
      sectionKey="solutions"
    />
  );
}
