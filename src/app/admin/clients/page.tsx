"use client";
import { SectionEditor } from "../components";

export default function ClientsAdmin() {
  return (
    <SectionEditor
      title="Clients (Đối tác)"
      description="Quản lý danh sách đối tác/khách hàng và header section"
      sectionKey="clients"
    />
  );
}
