"use client";
import { SectionEditor } from "../components";

export default function TestimonialsAdmin() {
  return (
    <SectionEditor
      title="Testimonials (Đánh giá)"
      description="Quản lý đánh giá từ học viên — avatar, tên, vai trò, nội dung"
      sectionKey="testimonials"
    />
  );
}
