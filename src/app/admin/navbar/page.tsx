"use client";
import { SectionEditor } from "../components";

export default function NavbarAdmin() {
  return (
    <SectionEditor
      title="Navbar"
      description="Quản lý logo, menu links và nút CTA trên thanh điều hướng"
      sectionKey="navbar"
    />
  );
}
